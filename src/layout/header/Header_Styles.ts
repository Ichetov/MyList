import styled from "styled-components"

const StyledHeader = styled.header`
background-color: rgba(44, 104, 25, 0.64);


position: fixed;
top:0;
width: 100%;
z-index: 1000;
display: flex;
padding: 20px 0;
`


export const S = {
    StyledHeader,
}